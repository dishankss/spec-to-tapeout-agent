You are a verification engineer.

Generate a single self-contained SystemVerilog testbench for the following design.

DUT Module Name: fp16_multiplier
Description: 16-bit floating-point multiplier (IEEE 754 half-precision compatible) with support for zero detection and rounding to nearest even. Takes two 16-bit FP16 inputs and computes the product in a single cycle.


Exact DUT module signature:
module fp16_multiplier(
  input  logic [15:0] a,
  input  logic [15:0] b,
  output logic [15:0] result
);


Parameters:
(none)

Ports:
- a (input) 16
- b (input) 16
- result (output) 16

Sample data / vectors:
sample_usage:
description: 'Example use case multiplying 1.0 * 2.0, expecting 2.0 in FP16 encoding.

  '
a: 16'b0_01111_0000000000
b: 16'b0_10000_0000000000
result: 16'b0_10000_0000000000

Requirements:
- Generate ONLY the testbench code.
- The testbench module name must be tb.
- Instantiate the DUT using the exact module name above.
- Use iverilog -g2012 compatible SystemVerilog.
- If the DUT is sequential and has clock/reset, drive them correctly.
- If sample outputs are available, print observed values using lines that start with OBS.
- If sample outputs are not available, create a compile-clean smoke-test bench that still exercises the interface.
- Do not include the DUT RTL.
- Do not include markdown fences.
- Do not rename DUT ports or parameters.
- Use reasonable delays / clock edges so the DUT can settle.

Use the provided sample data as the main stimulus source.
