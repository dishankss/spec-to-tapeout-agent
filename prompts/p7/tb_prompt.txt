You are a verification engineer.

Generate a single self-contained SystemVerilog testbench for the following design.

DUT Module Name: exp_fixed_point
Description: A 2-stage pipelined exponential function for unsigned fixed-point inputs using a 3-term Taylor  series approximation: e^x ≈ 1 + x + x^2/2 + x^3/6. Inputs are unsigned fixed-point values in  UQ1.(WIDTH-1) format (e.g., WIDTH = 8 implies UQ1.7). The output is in UQ(WIDTH+1).(WIDTH - 1) format.


Exact DUT module signature:
module exp_fixed_point #(
  parameter WIDTH = 8
) (
  input  logic               clk,
  input  logic               rst,
  input  logic               enable,
  input  logic [WIDTH-1:0]   x_in,
  output logic [2*WIDTH-1:0] exp_out
);


Parameters:
- WIDTH = 8

Ports:
- clk (input)
- rst (input)
- enable (input)
- x_in (input) WIDTH
- exp_out (output) 2*WIDTH

Sample data / vectors:
sample_usage:
description: 'Test input: x = 128 (which is 1.0 in UQ1.7). Expected output is approx.
  2.718 in UQ1.15 format, which is represented as 341 (rounded).

  '
clk_sequence:
- 1
- 0
- 1
- 0
- 1
- 0
- 1
rst_sequence:
- 1
- 0
- 0
- 0
- 0
- 0
- 0
enable_sequence:
- 1
- 1
- 1
- 1
- 1
- 1
- 1
x_in_sequence:
- 0
- 128
- 128
- 128
- 128
- 128
- 128
expected_exp_out:
- null
- null
- null
- 341

Requirements:
- Generate ONLY the testbench code.
- The testbench module name must be tb.
- Instantiate the DUT using the exact module name above.
- Use iverilog -g2012 compatible SystemVerilog.
- If the DUT is sequential and has clock/reset, drive them correctly.
- If sample outputs are available, print observed values using lines that start with OBS.
- If sample outputs are not available, create a compile-clean smoke-test bench that still exercises the interface.
- Do not include the DUT RTL.
- Do not include markdown fences.
- Do not rename DUT ports or parameters.
- Use reasonable delays / clock edges so the DUT can settle.

Use the provided sample data as the main stimulus source.
